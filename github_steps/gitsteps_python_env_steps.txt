First time Git activities
Create a github account and login 
Create a Git token ( password)
Install git on your laptop
Create a remote repo on git hub account 
Create a folder on your laptop
Open git bash on folder and execute below commands

If needed 
1.git config --global user.name "Ramesh Walake"
2.git config --global user.email "walakeramesh75@gmail.com"

echo "# myPython7am" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/aiwithmaha/myPython7am.git
git push -u origin main
(user name, password,token)

git clone https://github.com/walakeramesh/ai-data-engg-ramesh-learnings.git

create vertual environment
cd ai-data-engg-ramesh-learnings
pip install uv
uv python list
uv venv env --python cpython-3.11.1
env\Scripts\activate

For every Change 
Work directory   :  any change, change may be create, delete, update, edit 
Staging area       :  git add -A
Local repo          :  git commit -m “commnet” 
Remote repo       : git push

Install requirement models
uv pip install -r requirements.txt

select kernal -->
Ctrl + Shift + P, then:

Type Python: Select Interpreter.
Choose Enter interpreter path.
Click Find....

Select:

E:\workspace\ai-data-engg-ramesh-learnings\.venv\Scripts\python.exe
E:\workspace\ai-data-engg-ramesh-learnings\RagAiAcl\Scripts\python.exe

export UV_LINK_MODE=copy` or use `--link-mode=copy` to suppr